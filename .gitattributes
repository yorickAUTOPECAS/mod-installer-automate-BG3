# .bat/.cmd: NUNCA deixar o Git converter line endings (cmd.exe exige CRLF).
# -text = os bytes sao commitados e servidos exatamente como estao no disco.
*.bat -text
*.cmd -text
*.ps1 text eol=crlf

# Binarios: nunca alterar
*.zip binary
*.exe binary
*.pak binary
